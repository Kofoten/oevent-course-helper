name: Manual Runner Test
on: 
  workflow_dispatch: # This adds the "Run workflow" button you need

jobs:
  hello-narnia:
    runs-on: prioritise-test-run-courses-arc-runner # Must match your installation name
    steps:
      - name: Say Hello
        run: |
          echo "🎉 Success! This job is running on my home server."
          echo "Runner Name: $RUNNER_NAME"
          echo "Operating System: $(uname -a)"
